makeClass = makeClass || function() {};

makeClass(/** @lends app.Blog.prototype */{

	/**
	 * @constructs
	 */
	constructor: function() {

	},

	/**
	 * Write post
	 * @examplecode examples.blog.postExample
	 * @param {string} text
	 */
	post: function(text) {
	}

});
